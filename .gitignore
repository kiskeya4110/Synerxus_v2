node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Test coverage artifacts (auto-generated)
coverage/

# User uploads (should not be committed)
uploads/
!uploads/.gitkeep

# Environment files (contain secrets)
.env
.env.local
.env.*.local

# Local tool/IDE settings (may contain bash history with credentials)
.claude/
.cursor/
.vscode/settings.json
